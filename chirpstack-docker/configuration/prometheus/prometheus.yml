global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'lorawan-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:3005'] # Adjust to your actual NestJS port
